require 'pry'

class Initials
  def initialize(name_array)
    #binding.pry
    initials_array = name_array.map do |name|
      name[0]
    end

    puts initials_array.join
  end
end

Initials.new(ARGV)
